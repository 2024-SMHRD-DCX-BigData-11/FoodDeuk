<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.database.MemberMapper">
<insert id = "join" parameterType="Member">
	insert into t_user values (t_user_SEQ.NEXTVAL, #{user_id},#{user_pw},#{user_addr}, sysdate)
</insert>
<select id = "login" parameterType="Member" resultType="Member">
	select * from t_user where user_id=#{user_id} and user_pw=#{user_pw}
</select>
<select id ="idCheck" parameterType="String" resultType="boolean">
			select case when count(*)>0 then 1
				else 0
				end result
			from t_user where user_id=#{user_id}
		</select>
<select id = "loginName" parameterType="Member" resultType="String">
	select user_id from t_user where user_no=#{user_no}
</select>	
</mapper>