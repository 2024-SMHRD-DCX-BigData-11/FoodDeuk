<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.database.SearchMapper">
  	 <select id="searchByMenu" parameterType="Search" resultType="Restaurant">
  	 	SELECT * FROM t_restaurant WHERE res_no IN (SELECT DISTINCT(res_no) FROM t_menu WHERE menu_name LIKE '%' || #{menu_name} || '%')
            AND lat BETWEEN #{lat} - 0.005 AND #{lat} + 0.005
            AND lng BETWEEN #{lng} - 0.01 AND #{lng} + 0.01
    </select>

    <!-- 카테고리로 검색 -->
    <select id="searchByCategory" parameterType="Search" resultType="Restaurant">
    	SELECT * FROM t_restaurant WHERE res_no IN (SELECT res_no FROM t_restaurant_category WHERE category=(SELECT category_no FROM t_category WHERE category_name=#{category_name}))
            AND lat BETWEEN #{lat} - 0.005 AND #{lat} + 0.005
            AND lng BETWEEN #{lng} - 0.01 AND #{lng} + 0.01
    </select>
</mapper>