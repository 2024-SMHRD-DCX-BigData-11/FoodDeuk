<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.database.RestaurantMapper">
	<select id="listAll" resultType="com.smhrd.model.Restaurant">
  		SELECT * FROM t_restaurant
    </select>
    <select id = "resName" parameterType="Member" resultType="String">
	select res_name from t_restaurant where res_no=#{res_no}
</select>
<select id = "resNames" parameterType="Member" resultType="String">
	select res_name from t_restaurant where res_no=#{res_no}
</select>
</mapper>